"""Internal REST resource modules backing Cordless's flat bot.<verb>_<resource>() surface.

Not a public import path - everything here is reached through a Cordless
instance (see _mixin.py), the same way _aws.py/_multipart.py are internal.
"""
